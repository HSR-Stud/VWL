Volkswirtschaft
===============

Vorbereitung:

    $ git clone https://github.com/HSR-Stud/VWL
    $ cd VWL
    $ git submodule init
    $ git submodule update

Builden mit `Make` und `latexmk`:

    $ make
